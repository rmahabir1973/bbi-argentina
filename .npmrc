shamefully-hoist=false
strict-peer-dependencies=false

# Allow native build scripts for packages that require them
# pnpm blocks post-install scripts by default; these are safe native addons
approve-builds[]=sharp
approve-builds[]=esbuild
approve-builds[]=unrs-resolver
